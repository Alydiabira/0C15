{% extends 'admin.html.twig' %}

{% block admin %}
	<h1>Ajouter un invité</h1>

	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button type="submit">Créer</button>
	{{ form_end(form) }}
{% endblock %}
