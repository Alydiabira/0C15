{% extends 'admin.html.twig' %}


{% block admin %}
	<div class="container mt-4">

		<h1 class="mb-4">Modifier un média</h1>

		{{ form_start(form) }}
		{{ form_widget(form) }}

		<button class="btn btn-primary mt-3">Enregistrer</button>
		<a href="{{ path('admin_media_index') }}" class="btn btn-secondary mt-3">Retour</a>
		{{ form_end(form) }}

	</div>
{% endblock %}
